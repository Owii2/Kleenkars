[build]
command = ""          # no site build — just publish the repo root
publish = "."

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"
# DO NOT list external_node_modules here; let esbuild bundle everything.
